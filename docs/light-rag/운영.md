# LightRAG PoC 운영 메모

수시 갱신. 마지막 갱신 2026-08-29.

## 접속

| 항목 | 값 |
|---|---|
| WebUI / API | `http://192.168.0.103:31930` (`/webui`) — 사내망 전용 NodePort |
| API 키 | `lightrag-poc-sj-2026` (X-API-Key. WebUI는 최초 1회 입력 후 브라우저가 기억) |
| 파드 | `jh-wiki-graph` 네임스페이스 `lightrag` Deployment (공개 이미지 `ghcr.io/hkuds/lightrag`) |
| 앱 연동 | 사이드바 `LightRAG` 탭(/lightrag) → `/api/lightrag` 프록시 → `POST /query`. **bench 계정 전용**(탭 표시·API 모두, 0.71.0~) |
| LLM | gpt-5.6-luna ($0.20/M 입력 · $1.20/M 출력, 2026-08 기준). 임베딩은 사내 Ollama embeddinggemma(무료) |

## 시드 (색인)

```powershell
$env:FEDA_PW = '...'                        # docs/서버정보.md
py scripts/lightrag-seed.py --limit 30      # 사람·AI 문서(500자↑) 최근순
py scripts/lightrag-seed.py --slug <slug>   # 특정 문서
py scripts/lightrag-seed.py --query "질문"  # 색인 후 질의 테스트
```

운영 위키 DB(Postgres) 본문을 그대로 복사해 넣는다. 마스터 SSH 경유.

## 색인 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-19 | 최초 4건 — 빈 "무제" 2건(실수, 무해) + 주간 업무내역 2건 |
| 2026-08-20 | `--limit 30` — 28건 추가, 총 32건 전부 processed 확인. 주간업무 12·회사/거래처 6·인물 4·프로세스 8 |
| 2026-08-29 | `lightrag-wiki-seed.py --limit 5` — `wiki/` 최초 색인 (amicos·arabko·b-mote·barunweb·bio-fdnc), 전부 `processed` 완료 확인. 총 39건(기존 34 + 신규 5). 개발 PC VPN 재연결 후 실행(직전 시도는 VPN 끊겨 SSH 타임아웃) |
| 2026-08-29 | `lightrag-wiki-seed.py --limit 100` — `wiki/` 나머지 82건 제출. **6번째 문서에서 임베딩(Ollama) 60초 타임아웃 → 파이프라인 전체 halt → 나머지 76건 자동 취소** (77건 failed). 삭제 후 완전 순차(한 건 완료 대기 후 다음 제출)로 재시도 → **77건 전부 성공**. 최종 `processed: 121, failed: 0`(기존 34 + 오늘 87). 교훈: 대량 동시 투입 금지, 한 번에 여러 건 넣지 말 것 |
| 2026-08-29 | 무결성 검증(로컬 87건 vs 그래프 대조, 개수·중복·길이·제목) — **`daewon-graphic` 1건이 그래프에서 완전히 누락됨** 발견. 원인: 재시도 시 77건 삭제(비동기)를 5초만 기다리고 바로 재삽입 시작해 레이스 컨디션 발생. `--id daewon-graphic`으로 재색인 → 재검증 87/87 전부 정상 |
| 2026-08-31 | **납품 강화 재색인 완료** — 임베딩 OpenAI 전환(768→1536) + 동시성 8/4 + digest 고정 구성으로 121건 전량 재색인. 윈도우 4건 동시 투입, **실패 0** (이전 halt 사고 재발 없음). 속도 분당 4~5건 — 이전 구성(순차 2~4분/건) 대비 수 배. 무결성 검증 전 항목 정상(누락·중복·길이·failed 0), 질의 스모크 정확·9초/5.8초, 라벨 2,572개 중 성진 한글 핵심 라벨 유지. 구 PVC(lightrag-data) 삭제 완료. 도중 worker11 노드 장애로 postgres 이전 사고 1건(별건, k8s.yaml affinity로 조치) |
| 2026-08-31 | **emptyDir → PVC(cephfs-sc 10Gi) 전환** — `lightrag-data` PVC 생성, 파드 재생성으로 기존 색인 121건 소실(예정된 비용). 1건 색인 후 파드 강제 삭제로 생존 테스트 통과 → 전량 재색인 진행 (wiki 87 → 운영 34 순차) |
| 2026-08-31 | 벤치마크 15문항 (색인 121건 기준) — LightRAG 우세 3·기존 우세 2·둘 다 정확 10, 속도 2.6배(14.8s vs 38.3s), 날조성 오류는 기존 쪽만 2건. 도중 **LUNA 크레딧 소진**으로 14·15번 양쪽 실패 → 충전 후 재실행. 교훈: LightRAG "No relevant context"는 LLM 호출 실패(429)도 같은 문구 — 파드 로그 확인 필수. [벤치마크-2026-08-31.md](벤치마크-2026-08-31.md) |
| 2026-08-29 | "성진" 개체 영문화 원인 = `SUMMARY_LANGUAGE` 미설정. `deploy/k8s.yaml`에 `SUMMARY_LANGUAGE: Korean` 추가(`f118241`) 후 재배포 — emptyDir이라 기존 121건 색인 전부 소실. wiki 87건 + 기존 34건 전량 재색인(중간에 halt 2회, 완전 순차+인코딩 수정으로 복구) → 최종 `processed: 121, failed: 0`. 무결성 재검증(87/87 정상, 121건 중복 0) + 라벨·질의 재검증 완료 — [2026-08-29-summary-language-재검증.md](2026-08-29-summary-language-재검증.md) |

## 주의 (실측)

- **여러 건 동시 투입 금지** — 2026-08-29 82건을 한 번에 POST했더니 6번째 문서에서
  임베딩(Ollama, 직렬 처리) 60초 타임아웃 → **파이프라인 전체가 halt되며 나머지 76건이
  자동 취소**됐다(개별 실패가 아니라 전체 중단). 완전 순차(한 건 processed/failed
  확인 후 다음 제출)로 재시도하면 문제없다 — `lightrag-wiki-seed.py`로 대량 색인할 땐
  `--id`로 하나씩 돌리거나 스크립트에 순차 대기 로직을 넣을 것
- ~~**emptyDir** — 파드 재시작하면 색인 전부 소실~~ → **2026-08-31 PVC(cephfs-sc 10Gi) 전환 완료.**
  1건 색인 → 파드 강제 삭제 → 색인 생존 확인함. 이제 재시작해도 색인 유지.
  단, `image: latest`라 파드 재생성 시 LightRAG 버전이 올라갈 수 있음(태그 고정 검토)
- **속도** — 사용량 상한(MAX_ASYNC_LLM=2, MAX_PARALLEL_INSERT=1) 때문에 문서당 2~4분 순차.
  30건 ≈ 1시간+. 병렬 올려도 총비용 동일 — 대량 색인 전에 상한부터 올릴 것(재시작 = 색인 소실 주의)
- **같은 slug 재색인 거부** — "already contains" 응답. 갱신하려면 WebUI에서 삭제 후 재삽입
- **개체가 영어로 추출되는 경향** ("성진"→"Seongjin") — **해결**. 원인은
  `SUMMARY_LANGUAGE` 미설정(기본 English), `deploy/k8s.yaml`에 `SUMMARY_LANGUAGE: Korean`
  추가 후 전량 재색인으로 확인 완료 ([재검증 리포트](2026-08-29-summary-language-재검증.md)).
  단, `[[id]]` 위키링크 구문(영문 slug)이 본문에 그대로 노출되며 별개 영문 토큰으로
  잡히는 잔여 케이스는 남아 있음(SUMMARY_LANGUAGE와 무관, 손 안 댐)
- 색인 30건 비용 추정: 입력 25만~35만 + 출력 5만~8만 토큰 ≈ $0.11~0.17 (LUNA 로그·API로는
  토큰 실측 불가 — 정확한 값은 OpenAI 대시보드)

## 벤치마크

`py scripts/lightrag-bench.py [--n 3] [--mode mix]` — bench 계정으로 로그인해 같은 질문 15개를
LightRAG(`/api/lightrag`)와 기존 채팅(`/api/chat` ephemeral)에 던지고
`docs/light-rag/벤치마크-<날짜>.md`에 나란히 기록. 정확/우세 판정 열은 사람이 채운다.
bench 계정: bench / (BENCH_PW env, 기본 1234). 채팅이 문항당 1~3분 — 전체 수십 분.

## 질의 모드 5종

naive(원문 벡터만) · local(개체 중심) · global(주제·관계) · hybrid(local+global, 기본) ·
mix(hybrid+naive 전부). 상세와 모드별 추천 질문은 `검토.md` 5절과 원 리포 README `Query Param` 절.

## 제거 절차

`deploy/k8s.yaml`의 LightRAG 블록 삭제 + apply → 파드·색인 소멸. 앱 쪽은 `/lightrag` 탭·
`/api/lightrag`·configmap LIGHTRAG_* 두 줄 제거.
