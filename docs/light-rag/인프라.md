# LightRAG 인프라 구조 — 2026-08-31 기준

납품 강화판(digest 고정·OpenAI 임베딩·동시성 8/4·PVC v2) 반영. 접속 정보·색인 절차는 [운영.md](운영.md), 색인 개념은 [색인이란.md](색인이란.md).

## 전체 그림

```
[개발 PC (VPN 10.8.0.0/24)]
  │  scripts/lightrag-wiki-seed.py   ← wiki/ 87건 색인 (완전 순차)
  │  scripts/lightrag-seed.py        ← 운영 위키 단건 색인 (--slug)
  │  scripts/lightrag-bench.py       ← 벤치마크 15문항
  │
  ▼ NodePort 31930 (X-API-Key)
┌─────────────────────────────────────────────────────────────────┐
│ k8s 클러스터 (마스터 192.168.0.200 · worker01 192.168.0.103)      │
│ 네임스페이스 jh-wiki-graph                                        │
│                                                                 │
│  ┌───────────────────────────────┐      ┌────────────────────┐  │
│  │ lightrag Deployment           │      │ wiki-graph (본 앱)  │  │
│  │  ghcr.io/hkuds/lightrag       │◀─────│  /api/lightrag     │  │
│  │  port 9621                    │ 서비스 │  프록시 (bench 전용)│  │
│  │                               │ DNS   │  basePath /graphwiki│ │
│  │  WORKING_DIR=/data ──────┐    │      └────────────────────┘  │
│  │  SUMMARY_LANGUAGE=Korean │    │                ▲             │
│  │  MAX_ASYNC_LLM=8         │    │                │ 브라우저     │
│  │  MAX_PARALLEL_INSERT=4   │    │      cloud.fedaground.com    │
│  └──────────┬───────────────┼────┘      /graphwiki/lightrag     │
│             │               ▼                                   │
│             │   ┌────────────────────────┐                      │
│             │   │ PVC lightrag-data-v2   │  ← 2026-08-31 신설   │
│             │   │  cephfs-sc · 10Gi      │    (emptyDir에서 전환,│
│             │   │  그래프+벡터+문서상태    │     재시작 생존 확인)  │
│             │   └────────────────────────┘                      │
│             │                                                   │
│  ┌──────────▼──────────┐   ┌──────────────────────┐            │
│  │ postgres Deployment  │   │ PVC postgres-data    │            │
│  │  운영 위키 Page 테이블│───│  cephfs-sc · 20Gi    │            │
│  └─────────────────────┘   └──────────────────────┘            │
└─────────────────────────────────────────────────────────────────┘
        │                                  │
        ▼ 색인·질의 시 LLM 호출              ▼ 색인·질의 시 임베딩
┌──────────────────────┐        ┌───────────────────────────┐
│ LUNA (gpt-5.6-luna)  │        │ OpenAI 임베딩              │
│  api.openai.com      │        │  text-embedding-3-small   │
│  Secret wiki-graph-llm│        │  1,536차원 · 병렬 안전      │
│  크레딧 소모 (유료)     │        │  ~$0.02/100만 토큰(≈0)     │
└──────────────────────┘        └───────────────────────────┘
```

## 구성 요소

| 구성 | 값 | 비고 |
|---|---|---|
| LightRAG 서버 | `ghcr.io/hkuds/lightrag@sha256:ab23a9c8…` (core 1.5.6) | **digest 고정** — 재시작해도 같은 버전. 업그레이드는 digest 명시 갱신으로 |
| 노출 | NodePort `192.168.0.103:31930` · 클러스터 내부 `http://lightrag:9621` | X-API-Key 인증 (`lightrag-poc-sj-2026`) |
| 저장소 | PVC `lightrag-data-v2` (cephfs-sc, 10Gi) → `/data` | **재시작 생존.** 1건 색인 → 파드 강제삭제 → 유지 확인함 |
| 추출·생성 LLM | LUNA gpt-5.6-luna (OpenAI 바인딩) | 색인 ②단계·질의 응답. 크레딧 소진 시 색인·질의 전부 실패 ("No relevant context"로 위장) |
| 임베딩 | OpenAI text-embedding-3-small 1,536d | 2026-08-31 사내 Ollama(직렬, halt 3회)에서 전환. 병렬 안전, 비용 ≈0 |
| 동시성 상한 | `MAX_ASYNC_LLM=8` · `MAX_PARALLEL_INSERT=4` | 2026-08-31 상향 (임베딩 병렬 안전 전제). 색인 실측 분당 4~5건 |
| 추출 언어 | `SUMMARY_LANGUAGE=Korean` | 2026-08-29 추가 — 미설정 시 개체가 영어로 뽑힘 |
| 앱 연동 | `/api/lightrag` 프록시 (bench 계정 전용) | GET status·documents·labels + POST query(references 포함) |
| 확인 화면 | `/graphwiki/lightrag` (bench 로그인) | 상태 줄·질의 5모드·대화창·청크 검색(원자료)·색인 문서 표·라벨 검색 |

## 색인 데이터 (121건)

| 출처 | 건수 | 시드 경로 |
|---|---|---|
| 큐레이션 위키 `docs/light-rag/wiki/` (`status: reviewed`만) | 87 | `lightrag-wiki-seed.py` — 한 건 완료 확인 후 다음 제출 |
| 운영 위키 (Postgres `Page`, user·agent 작성 500자↑ 최근순) | 34 | `lightrag-seed.py --slug` 단건 (일괄 `--limit`은 halt 사고 이력 — 운영.md) |

## 요청 흐름

- **색인**: 개발 PC → NodePort `POST /documents/text` → 청킹(파드 내부, LLM 무관) → 개체·관계 추출(LUNA) → 임베딩(OpenAI) → PVC 저장
- **질의(화면)**: 브라우저 → `/graphwiki/api/lightrag` → 앱 서버가 서비스 DNS로 `POST /query` → 키워드 추출(LUNA) → 그래프+벡터 검색(PVC) → 답변 생성(LUNA)
- **질의(직접)**: 개발 PC → NodePort `POST /query` (VPN 필요)

## 알려진 운영 리스크

1. **LUNA 크레딧 소진** — 색인·질의 전부 죽고 오류가 "No relevant context"로 위장됨. 파드 로그로만 구분 가능 (2026-08-31 실제 발생)
2. ~~임베딩 직렬 병목~~ → OpenAI 임베딩 전환으로 해소. 다만 색인 동시 투입은 여전히 윈도우(4) 이내 유지 권장
3. ~~image: latest~~ → digest 고정으로 해소
4. VPN 불안정 — 개발 PC發 색인·조회가 끊기면 SSH(마스터 경유)도 같이 끊김. 파드 내부 동작엔 무관
