services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: wiki
      POSTGRES_USER: wiki
      POSTGRES_DB: wiki
    ports: ["15432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
